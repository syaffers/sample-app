<% if activity.trackable %>
    <%= activity.owner.name if activity.owner %>
    added a paper:
    <%= link_to activity.trackable.title, activity.trackable %>
    in 
    <%= activity.trackable.subject.name %>
    <%= time_ago_in_words(activity.trackable.created_at) %> ago
<% else %>

<% end %>